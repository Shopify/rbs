#ifndef RBS__RUBY_OBJS_H
#define RBS__RUBY_OBJS_H

#include "ruby.h"

<%- nodes.filter(&:expose_to_ruby?).each do |node| -%>
VALUE <%= node.c_function_name %>(<%= node.fields.map { |f| "VALUE #{f.name}" }.join(", ") %>);
<%- end -%>

#endif
